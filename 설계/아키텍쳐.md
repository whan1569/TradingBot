```mermaid
graph TD;
    A[Main System] -->|Runs| B[Auto Trader];
    A -->|Monitors| C[Dashboard];
    A -->|Logs Data| D[Data Storage];
    
    B -->|Executes Trades| E[Execution Module];
    B -->|Backtests Strategies| F[Backtest Engine];
    B -->|Applies Strategies| G[Trading Strategies];
    
    E -->|Manages Orders| E1[Order Execution];
    E -->|Risk Control| E2[Risk Management];
    E -->|Adaptive Execution| E3[Adaptive Execution Strategy];
    
    F -->|Optimizes| F1[Optimization];
    F -->|Stores Results| F2[Test Results];
    
    G -->|Sentiment Analysis| G1[Market Sentiment];
    G -->|Chart Analysis| G2[Chart Analysis];
    G -->|Signal Processing| G3[Signal Processing];
    G -->|Dynamic Model Selection| G4[Adaptive Model Selector];
    G -->|Event Detection| G5[Event Detector];
    
    C -->|Streams Data| H[WebSocket Handler];
    C -->|Caches Data| I[Redis Cache];
    C -->|Displays| J[Streamlit Dashboard];
    
    D -->|Stores Raw Data| D1[Raw Data];
    D -->|Processes Data| D2[Processed Data];
    D -->|Backtest Data| D3[Historical Data];
    
    subgraph Execution
        E1
        E2
        E3
    end
    
    subgraph Strategies
        G1
        G2
        G3
        G4
        G5
    end
    
    subgraph Data
        D1
        D2
        D3
    end

    subgraph Backtest
        F1
        F2
    end
    
    subgraph Dashboard
        H
        I
        J
    end