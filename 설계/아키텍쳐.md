```mermaid
graph TD;
    A[Main System] -->|Loads Config| B[Configuration Module];
    A -->|Collects Data| C[Data Module];
    A -->|Executes Trades| D[Execution Module];
    A -->|Runs Backtest| E[Backtest Module];
    A -->|Updates Dashboard| F[Dashboard Module];

    B -->|Loads API Keys| B1[API Keys];
    B -->|Loads Strategy Config| B2[Strategy Config];
    B -->|Loads General Settings| B3[General Config];

    C -->|Fetches Market Data| C1[Market Data Collector];
    C -->|Processes Data| C2[Data Processor];
    C -->|Stores Data| C3[Data Storage];
    C3 -->|Parquet Storage| C3a[Historical Data];
    C3 -->|Cache| C3b[Redis Cache];

    D -->|Trade Execution| D1[Trade Executor];
    D -->|Manages Risks| D2[Risk Manager];
    D -->|Optimized Order Execution| D3[Adaptive Execution];

    E -->|Runs Backtests| E1[Backtest Engine];
    E -->|Optimizes Strategy| E2[Optimization Module];

    F -->|Displays Charts| F1[Chart Visualizer];
    F -->|Tracks Portfolio| F2[Portfolio Tracker];
    F -->|Analyzes Sentiment| F3[Sentiment Analysis];
    F -->|Fetches & Updates Data| F4[Data Fetcher];

    C1 -->|Uses| G[WebSocket Handler];
    C1 -->|Uses| H[API Connector];

    subgraph Strategies
        S1[Market Sentiment]
        S2[Chart Analysis]
        S3[Signal Processing]
        S4[Trading Decision]
        S5[Adaptive Model Selector]
        S6[Event Detector]
    end

    C2 -->|Uses| Strategies;
    D1 -->|Uses| Strategies;
    E2 -->|Optimizes| Strategies;

    subgraph Models
        M1[Sentiment Model]
        M2[Auto Optimization]
        M3[Reinforcement Learning]
    end

    S5 -->|Uses| Models;
    S6 -->|Triggers| S5;

    F4 -->|Uses| Redis Cache;

    classDef module fill:#f9f,stroke:#333,stroke-width:2px;
    class A,B,C,D,E,F module;
